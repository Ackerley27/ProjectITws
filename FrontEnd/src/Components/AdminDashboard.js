const AdminDashboard = () => {
    const handleAddIceCream = () => {
        // โค้ดสำหรับเพิ่มไอติม
    };

    return (
        <div>
            <h2>Admin Dashboard</h2>
            <button onClick={handleAddIceCream}>Add New Ice Cream</button>
        </div>
    );
};

export default AdminDashboard;
