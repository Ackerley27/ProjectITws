const UserView = () => {
    const handleOrder = (id) => {
        // โค้ดสำหรับสั่งซื้อไอติม
    };

    return (
        <div>
            <h2>Order Ice Cream</h2>
            <button onClick={() => handleOrder(1)}>Order Ice Cream</button>
        </div>
    );
};

export default UserView;
